package com.parkinglot;

public class ParkingLotOwner {
    public void full(boolean fullStatus) {
        boolean full = fullStatus;
    }

    public void availableSpace(int spaces) {
        System.out.println("Available spaces:" + spaces);
    }
}
